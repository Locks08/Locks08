# slides/

提案資料の画像をここに配置してください。

- `slide-01.png` 〜 `slide-09.png`（全9枚）
- `slide-07.png` は S7「ポジション番号ルールの図」としても本文中に表示されます

ファイル名は上記の通り固定です（`index.html` が参照します）。
画像を置けば、付録セクション（S12）に自動でグリッド表示され、クリックで拡大（lightbox）します。
