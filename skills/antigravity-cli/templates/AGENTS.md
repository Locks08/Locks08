# AGENTS.md

Workspace context for the Antigravity CLI (`agy`). Copy to the repository root and fill in.
Keep it short — it is prepended to every agent turn and competes with skills and MCP tools
for the customization token budget (user/workspace rules get a dedicated 20k budget; oversized
rules are cut on newline boundaries).

## Project

- What this repository is:
- Primary language / framework:
- Entry points:

## Commands

```bash
# install
# build
# test
# lint / format
```

## Conventions

- Style:
- Naming:
- Directory layout rules:
- Commit message format:

## Boundaries

- Never touch:
- Requires human approval:
- Generated files (regenerate, never hand-edit):

## Definition of done

- [ ] Build passes
- [ ] Tests pass
- [ ] Lint/format clean
- [ ] No unrelated files changed
